npx create-react-app my-app
cd my-app
npm install react-router-dom

npm start

//npm install --save-dev gh-pages

npm install -g gh-pages

package.json
{
    "homepage": "https://<YourUsername>.github.io/<YourRepoName>",

    "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build"
  },
}

npm run deploy


On GitHub:

Go to your repository (the one named e1).
Click Settings → Pages (in the sidebar or under “Code and automation” in newer layouts).
Under Build and Deployment (or “Source”), confirm:
Branch is gh-pages (or it should say that it’s being served from the gh-pages branch).
Folder is / (root) if shown.


https://drummian.github.io/e1